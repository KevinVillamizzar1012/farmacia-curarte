spring:
  datasource:
    url: jdbc:mysql://localhost:3306/farmacia_db?useSSL=false&serverTimezone=America/Bogota
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        globally_quoted_identifiers: true

jwt:
  secret: ${JWT_SECRET:miClaveSuperSecretaParaFarmaciaCurarte2026ConMasDe32Caracteres}
  expiration: 86400000
  logging:
  level:
    org.springframework.security: DEBUG